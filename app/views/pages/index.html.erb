<h1>Pages#index</h1>
<p>Find me in app/views/pages/index.html.erb</p>

<h2>新規登録<h2>
  <%# <% if @goal %>
    <%# <% if @goal.errors.any? %>
      <%# <%= @goal.errors.full_messages %>
    <%# <% end %>
  <%# <% end %>

  <%= form_with model: [ @goal, Goal.create ] do |form| %>
  <%# <%= form_with model: [ @goal, Page.create ] do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title, class: 'form-control', id: 'goal_title'%>
  </div>

  <div>
    <%= form.label :description %>
    <%= form.text_area :description, row: 5, class: 'form-control', id: 'goal_description' %>
  </div>

  <div>
    <%= form.submit nil, class: 'btn btn-primary'%>
  </div>
<% end %>

<h2>目標一覧<h2>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @goals.each do |goal| %>
      <tr>
        <td><%= link_to goal.id, goal_path(goal.id) %></td>
        <td><%= goal.title %></td>
        <td><%= goal.description %></td>
        <td><%= link_to "edit", edit_goal_path(goal.id) %></td>
        <td><%= link_to "Delete", goal_path(goal.id), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>